build/
dist/
.clickable/
*.click

# Generated by gecko/android-libs-shim/regenerate.sh: per-architecture outputs
# (two ELF files and three ld scripts naming an absolute path), see gecko/README.md.
/gecko/android-libs-shim-arm64/
/gecko/android-libs-shim/liblog.so
/gecko/android-libs-shim/libatlndkstub.so
/gecko/android-libs-shim/libandroid.so
/gecko/android-libs-shim/libjnigraphics.so
/gecko/android-libs-shim/libmediandk.so
/gecko/atlas-libandroid-arm64.so

# CI's image job drops libfenix.so here for build.sh to pick up (build.yml).
/prebuilt/

# which vehicle a build stages, written by CI beside the build (build.sh).
.vehicle
